user = ${node.metadata.get('postfix', {}).get('database', {}).get('username', 'vmail')}
password = ${node.metadata.get('postfix', {}).get('database', {}).get('password', repo.libs.pw.get("mysql_vmail_user_{}".format(node.name)))}
dbname = ${node.metadata.get('postfix', {}).get('database', {}).get('database', 'vmail')}
hosts = ${node.metadata.get('postfix', {}).get('database', {}).get('host', '127.0.0.1')}
query = select concat(destination_username, '@', destination_domain) as destinations from aliases where source_username = '%u' and source_domain = '%d' and enabled = true;